apiVersion: v1
kind: Service
metadata:
    name: "rogoyalgua-493f"
    labels:
        app: "rogoyalgua-493f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "rogoyalgua-493f"